# docker/orchestrator/requirements.txt
# Runtime deps for the orchestrator container.
# Excludes Numba, SciPy, torch — only needed by match-engine.
# Includes aiodocker for Docker socket management.

# HTTP / WebSocket (Goalserve + Kalshi client)
httpx==0.27.0
websockets==12.0

# Database
asyncpg==0.29.0
redis==5.0.3

# Docker SDK (async) — launches/monitors match-engine containers
aiodocker==0.22.2

# Observability
structlog==24.1.0
prometheus-client==0.20.0

# Config
pyyaml==6.0.1

# Numerical (lightweight — used by Phase 2 pipeline inside orchestrator)
numpy==1.26.4
scipy==1.12.0
xgboost==2.0.3
